---
kind: pipeline
type: docker
name: Test

steps:
  - name: Run Tests
    image: rust:latest
    commands:
      - rustup component add clippy
      - cargo test
      - cargo clippy
trigger:
  trigger:
    event:
      - push
      - pull_request
